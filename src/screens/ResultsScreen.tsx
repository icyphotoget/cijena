import { View, Text } from 'react-native';

export default function ResultsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Rezultati (sljedeće dodajemo)</Text>
    </View>
  );
}
